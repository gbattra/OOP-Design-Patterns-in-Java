package codes.application;

public interface EncoderController<K, S> {
  boolean loadEncoder(String filepath);
//  boolean createEncoder(K codes, S symbols);
//  boolean saveEncoder(String filepath);
//  K encode(S sequence);
//  S decode(K sequence);
}
